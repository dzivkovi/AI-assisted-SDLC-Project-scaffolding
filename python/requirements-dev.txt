# Development tooling. The actual versions live in pyproject.toml under
# [project.optional-dependencies] dev, so there is one source of truth.
#
#   pip install -r requirements-dev.txt
#
# is equivalent to `pip install -e ".[dev]"`.
-e .[dev]
